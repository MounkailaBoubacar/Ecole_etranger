{% extends 'base.html.twig' %}

{% block title %}Hello MainController!{% endblock %}

{% block body %}
    <h1><span>Classe </span></h1>
    <div class="text-center text-lg-start">
        <a href="add" class="btn-get-started scrollto">Ajouter une matiere</a>
    </div>
    <table class="table table-bordered table-dark table-striped">
        <tr>
            <th>Identifiant</th>
            <th>nom matiere</th>
            <th>Coefficient</th>
            <th>Proffeseur</th>
            <th>Modifier</th>
        </tr>

        {% for liste in liste %}
        <tr >
            <td>{{liste.id}}</td>
            <td>{{liste.nom}}</td>
            <td>{{liste.coefficient}}</td>
            <td>{{liste.user}}</td>
            <td> <a href="{{path('admin_matiere_edit',{id:  liste.id})}}">Valider</a> </td>
            {% endfor %}
        </tr>
    </table>
{% endblock %}
